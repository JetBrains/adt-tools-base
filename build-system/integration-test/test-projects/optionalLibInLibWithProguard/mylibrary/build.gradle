apply plugin: 'com.android.library'

android {
    compileSdkVersion 22 // Tests depend on Apache HTTP, TODO: use useLibrary.
    buildToolsVersion = rootProject.buildToolsVersion

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 23
    }
}
