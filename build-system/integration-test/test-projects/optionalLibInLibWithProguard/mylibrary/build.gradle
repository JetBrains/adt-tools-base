apply plugin: 'com.android.library'

android {
    compileSdkVersion 22
    buildToolsVersion = rootProject.buildToolsVersion

    defaultConfig {
        minSdkVersion 23
        targetSdkVersion 23
    }
}
