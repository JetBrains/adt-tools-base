apply plugin: 'android'

android {
    compileSdkVersion 17
    buildToolsVersion "19.0.2"

    defaultConfig {
        renderscriptTargetApi = 11
    }
}

dependencies {
    compile project(':lib')
}
