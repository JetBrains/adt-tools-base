apply plugin: 'android-library'

dependencies {
    compile project(':lib2:libs')
}

android {
    compileSdkVersion 19
    buildToolsVersion '19.1.0'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 15
    }
}