apply plugin: 'android-library'

dependencies {
    compile project(':lib2:libs')
}

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.2"

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 15
    }
}