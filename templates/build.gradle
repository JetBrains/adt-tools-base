apply plugin: 'sdk-files'

sdk {
    common {
        path('projects')   { into 'templates/projects' }
        path('activities') { into 'templates/activities' }
        path('gradle')     { into 'templates/gradle' }
        path('other')      { into 'templates/other' }
    }
}